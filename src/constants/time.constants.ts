/**
 * Time constants for the application
 */

// Time after which the user is considered idle but working without a timer (in milliseconds)
// Default: 15 minutes
export const IDLE_REMINDER_THRESHOLD = 15 * 60 * 1000;

// Interval for timer tick (in milliseconds)
export const TIMER_TICK_INTERVAL = 200;
